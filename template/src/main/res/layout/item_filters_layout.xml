<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="wrap_content"
    android:layout_height="match_parent"
    android:id="@+id/main_layout"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <androidx.constraintlayout.utils.widget.ImageFilterView
        android:id="@+id/ifv_filter"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toTopOf="@id/mtv_filter_name"
        app:srcCompat="@drawable/img_template_anniversary_thumbnail_1"
        android:adjustViewBounds="true"/>

    <com.google.android.material.textview.MaterialTextView
        android:id="@+id/mtv_filter_name"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toBottomOf="@id/ifv_filter"
        app:layout_constraintBottom_toBottomOf="parent"
        tools:text="Base"
        android:textColor="@color/white_color"
        tools:textColor="@color/black_color"/>

</androidx.constraintlayout.widget.ConstraintLayout>